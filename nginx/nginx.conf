server {
    listen 80;

    location /static/ {
        alias /staticfiles/;
        try_files $uri =404;
    }

    location /media/ {
        alias /media/;
        try_files $uri =404;
    }

    location / {
        proxy_pass http://backend:8000;
    }
}